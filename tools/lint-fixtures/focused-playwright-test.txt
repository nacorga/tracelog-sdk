import { test, expect } from "@playwright/test";

test.describe.only("the browser floor", () => {
  test("loads the runtime", async ({ page }) => {
    await expect(page).toHaveTitle(/TraceLog/);
  });
});
