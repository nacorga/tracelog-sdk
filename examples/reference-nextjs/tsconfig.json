{
  "extends": "../../tsconfig.package.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "rootDir": "${configDir}"
  },
  "include": ["${configDir}/src/**/*.ts", "${configDir}/e2e/**/*.ts"]
}
