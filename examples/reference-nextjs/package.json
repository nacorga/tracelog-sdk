{
  "name": "@tracelog/reference-nextjs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node build.mjs",
    "pretest:e2e": "turbo run build --filter=@tracelog/reference-nextjs",
    "pretest:integration": "turbo run build --filter=@tracelog/reference-nextjs --filter=@tracelog/api --filter=@tracelog/db",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:integration": "playwright test --config playwright.integration.config.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@tracelog/capture-web": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@tracelog/api": "workspace:*",
    "@tracelog/db": "workspace:*",
    "@tracelog/detectors": "workspace:*",
    "@tracelog/domain": "workspace:*",
    "@tracelog/event-contract": "workspace:*",
    "@tracelog/platform-testkit": "workspace:*",
    "@tracelog/testkit": "workspace:*"
  }
}
